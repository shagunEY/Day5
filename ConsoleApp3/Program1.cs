using System.Threading.Tasks;

namespace ConsoleApp3
{
    /*
    class Calculator
    {
        //adding 2 integer values
        public int Add(int a, int b)
        {
            int sum = a + b;
            return sum;
        }
        
        //adding three integer values
        public int Add(int a, int b, int c)
        {
            int sum = a + b + c;
            return sum;
        }
        public double Add(double a, double b, double c)
        {
            double sum = a + b + c;
            return sum;
        }
        public int Subtract(int a, int b)
        {
            int subtract = a - b;
            return subtract;
        }

        public int Subtract(int a, int b, int c)
        {
            int subtract = a - b - c;
            return subtract;
        }

        public int Multiply(int a, int b)
        {
            int multiply = a * b;
            return multiply;
        }

        public int Multiply(int a, int b, int c)
        {
            int multiply = a * b * c;
            return multiply;
        }

        //Main method
        public static void Main(String[] args)
        {
            //Creating Object
            Calculator ob = new Calculator();

            int sum1=ob.Add(1, 2);
            Console.WriteLine("Sum of two integer value : " + sum1);

            int sum2 = ob.Add(1, 2, 3);
            Console.WriteLine("Sum of three integer value : " + sum2);

            double sum3 = ob.Add(1.3, 2.2, 3.4);
            Console.WriteLine("Sum of three decimal values: " + sum3);

            int subtract1=ob.Subtract(1, 2);
            Console.WriteLine("Subtraction of two integer values : " + subtract1);

            int subtract2=ob.Subtract(1, 2, 3);
            Console.WriteLine("Subtraction of three intger values : " + subtract2);

            int multiply1=ob.Multiply(1, 2);
            Console.WriteLine("Multiplication of two integer values : " +  multiply1);

            int multiply2 = ob.Multiply(1, 2, 3);
            Console.WriteLine("Multiplication of three integer values : " + multiply2);
        }
    }
    */
}